%%HP: T(0)A(D)F(.);
´ ç U
  ´ { V Z } PURGE U Y à X RISCH NEG U X à Y RISCH + EVAL V STO 'V' V = U V i * + EVAL Z STO "Harmonica"
    IF Z X à X à X 1 = SUBST Y 1 = SUBST EVAL Z Y à Y à X 1 = SUBST Y 1 = SUBST EVAL + 0 ==
    THEN
    ELSE "Nao " SWAP +
    END
  ª
ª
