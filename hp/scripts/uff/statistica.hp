@ ============================================================================
@ stat
@ ============================================================================
DIR

  CST
  {{\|^  \<< UPDIR Loader \>> }
    { + P}
    {C COMB}
    {A PERM}
    {R RAND}
  }

  load \<< Load \>>

END 'stat' STO
stat
@UPDIR
@ ============================================================================

@ ============================================================================
@ ->P
@ ============================================================================
DIR

  CST
  {{\|^  \<< UPDIR Loader \>> }
   {op \<< 44.2 KEYEVAL 15.1 KEYEVAL \>> }
  }

  load \<< Load \>>

END 'P' STO
@ ============================================================================

